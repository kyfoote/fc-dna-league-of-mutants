// Entry point — index.html loads dist/main.js, which is this file compiled.
// Its only job is to start the game; everything else lives in game.ts plus the
// scenes/, entities/ and assets/ folders.
import './game.js';
